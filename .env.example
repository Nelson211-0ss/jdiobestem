# Copy to .env for local serverless dev (do not commit .env)
# Stripe Dashboard → Developers → API keys (use sk_test_… until go-live)

STRIPE_SECRET_KEY=sk_test_your_secret_key

# Optional: canonical site URL for success/cancel links if auto-detection fails
# PUBLIC_SITE_URL=https://www.yoursite.org
